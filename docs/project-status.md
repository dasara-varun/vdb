# VDB Project Status

**Status:** Living snapshot
**Last reviewed:** 2026-08-24
**Repository state:** Verify with `git rev-parse HEAD` and `git status --short --branch` at each handoff
**Maintainer rule:** Update this file whenever implementation status, validation state, priorities, or known limitations change.

## Executive summary

VDB is a Rust-based, local-first document NoSQL database MVP. It uses a versioned `VDB1` file header, append-only CBOR WAL records, per-record SHA-256 checksums, bounded streaming replay, semantic WAL validation, private Unix file modes, common-path symlink rejection, optimistic document versions, bounded equality queries and imports, equality indexes, health and schema reports, verified backups, JSON Lines portability, safe version-preserving compaction, a deterministic read-only Steward, and an opt-in loopback-only read-only browser GUI for local inspection.

The project is **not production-ready**. Its current state is deliberately small and inspectable. Encryption at rest, authenticated encryption, OS-level advisory locking, stale-lock recovery, memory-bounded storage, a query planner, application API, authentication, replication, and a model-backed Steward remain future work.

## Capability status

| Area | Status | Evidence or source of truth |
|---|---|---|
| Rust workspace and CLI | Implemented | `crates/vdb-core`, `crates/vdb-cli`, `README.md` |
| Opt-in local read-only GUI | Implemented baseline; loopback-only embedded interface, no authentication or mutation | `crates/vdb-cli/src/gui.rs`, ADR-0002, GUI tests and smoke script |
| `VDB1` header/version validation | Implemented for versions 1–2, with version-preserving compaction snapshots | `docs/data-format.md`, core tests |
| Append-only CBOR WAL | Implemented | `docs/data-format.md`, core implementation |
| Record checksum and replay recovery | Implemented baseline with bounded streaming and semantic checks | `docs/data-format.md`, corruption and adversarial tests |
| Document CRUD and optimistic versions | Implemented | `docs/api.md`, core tests |
| Bounded equality query | Implemented baseline for top-level and dotted nested equality fields | `docs/api.md`, query tests |
| Single-field equality index | Implemented baseline | `docs/performance.md`, index tests |
| Health and schema reports | Implemented | CLI and core tests |
| Verified backup and JSON Lines portability | Implemented baseline; backup verification requires an intact manifest and import is prevalidated/atomic | `docs/operations.md`, backup/export/import tests |
| WAL compaction | Implemented safer version-preserving baseline | `docs/data-format.md`, compaction regression test |
| Configurable WAL storage quota | Implemented bounded write-time guard; default 512 MiB, configurable up to 16 GiB; compaction replacement is also bounded | `docs/api.md`, core quota and compaction-quota regression tests |
| Cross-platform installation/release workflow | Implemented baseline artifact workflow and device guide; native workspace tests now run on Ubuntu, macOS, and Windows, while target-platform durability validation remains pending | `.github/workflows/release.yml`, `.github/workflows/ci.yml`, `docs/installation.md` |
| Startup process lock ordering | Implemented baseline; Unix uses safe advisory locking and stale regular lock files are recoverable; non-Unix fallback remains conservative | `docs/operations.md`, lock regression tests |
| Deterministic read-only Steward | Implemented baseline | `docs/steward.md`, CLI output |
| Encryption/key management | Planned, P0 | `docs/improvement-plan.md`, `docs/security.md` |
| Authentic committed dependency lockfile | Implemented baseline; stable-Cargo lockfile is committed, checksums audited against official sparse crates.io metadata, hosted locked validation is green, and cargo-deny policy is configured with maintained `ciborium` replacing unmaintained `serde_cbor`; hosted revalidation of this policy fix is pending | `Cargo.lock`, `deny.toml`, `docs/testing.md`, CI locked commands/freshness/audit/policy jobs |
| Memory-bounded segments/checkpoints | Planned, P1 | `docs/improvement-plan.md` |
| Local application API | Planned, P1 | `docs/roadmap.md`, `docs/improvement-plan.md` |
| Model-backed Steward | Planned, P2 | `docs/steward.md`, `docs/security.md` |
| Replication | Deferred, P2 | `docs/roadmap.md`, `docs/production-gap-audit.md` |
| Documentation system | Implemented baseline | `AGENTS.md`, `CONTRIBUTING.md`, `docs/README.md`, `docs/documentation-maintenance.md` |
| Agent-tooling and bounded-loop guidance | Implemented baseline; optional and non-authoritative | `docs/agent-tooling.md`, `docs/loop-engineering.md`, `CLAUDE.md` |
| Security-conscious tool inventory | Implemented for the current development environment; refresh on tool changes | `docs/agent-tooling.md` |
| Filesystem and input hardening | Implemented baseline with strict backup paths and atomic bounded imports | `docs/security.md`, core adversarial tests |
| Directory-entry synchronization | Implemented best-effort Unix parent-directory `sync_all` after creation and replacement; non-Unix remains unvalidated | `docs/durability-matrix.md`, core durability boundaries |
| Process-crash recovery smoke | Implemented bounded Unix CLI interruption smoke; full cross-platform and power-loss qualification remains open | `scripts/crash-smoke.sh`, `docs/testing.md`, hosted CI |
| Materialized document-count budget | Implemented configurable count cap for writes and replay; byte-level working-set bounds remain open | `VdbOptions`, `--max-documents`, quota regression |
| Aggregate payload-byte budget | Implemented configurable aggregate document-payload cap for writes, imports, replay, and health reporting; index/RSS accounting remains open | `VdbOptions`, `--max-payload-bytes`, quota regression |
| Semantic WAL replay validation | Implemented baseline | `docs/data-format.md`, version and corruption tests |

## Validation snapshot

The current local validation passed formatting, Clippy with warnings denied, **34 core tests and 20 CLI/GUI tests**, workspace tests, the release GUI smoke workflow, and the restore/advisory-lock/durability regression suite. The latest hosted policy attempt failed only because cargo-deny correctly identified the unmaintained `serde_cbor` dependency and the intentional private workspace path dependency under the initial wildcard policy; the working remediation replaces the former with maintained `ciborium` and uses cargo-deny’s documented private-path exception while retaining external wildcard denial. A new hosted run is required before this policy cycle can be marked green. The committed stable-Cargo Cargo.lock has 101 package entries and 99 registry checksums audited against official sparse crates.io metadata; the Debian-based sandbox still reports replacement-source checksum comparisons for local locked builds, while hosted CI run `32748622080` passed locked Ubuntu/macOS/Windows tests, lockfile freshness, GUI smoke, and RustSec auditing. The broader previous validation also passed shell syntax and documentation consistency checks, release compilation, and the CLI lifecycle smoke workflow. The current adversarial tests cover oversized and atomic imports, multiple-record JSON Lines import, oversized WAL length prefixes failing closed, duplicate snapshot rejection, deterministic truncated-tail recovery at every final-record byte boundary with valid-prefix preservation, invalid WAL version sequences, legacy format reopening, unsupported-format rejection, strict backup-manifest verification including symlink rejection, backup self-target rejection, backup/export overwrite protection, Unix private modes for database/lock/backup/manifest files, bounded document and WAL configuration, larger configured documents, nested indexed and unindexed equality queries, no-partial-append quota failure, concurrent-open rejection, stale Unix lock-file recovery, verified restore-to-new-path with destination manifest creation, and refusal to modify an existing destination manifest. Prior hosted validations include run `32706132858` for `d8d97bf` (`test: document and cover truncated WAL recovery`), run `32707712911` for `b833a72` (`fix: harden import queries and backup verification`), run `32727612212` for `a4ce7a7` (`fix: fail closed on malformed replay records`), run `32729196952` for `d1bb9c1` (`test: expand durability recovery evidence`), run `32730657419` for `64c90a2` (`fix: add unix advisory locking`), run `32731710061` for `323e78b` (`test: satisfy hosted restore lint`), and run `32732332942` for `587b2cd` (`fix: preflight restore destinations`). Native platform matrix commit `984322e` (`ci: test workspace across native platforms`) passed hosted VDB CI run `32732881379` on the configured Ubuntu, macOS, and Windows test matrix. GUI milestone commit `cb17489` (`feat: add secure local read-only GUI`) passed hosted VDB CI run `32736174539`, including documentation, format, Clippy, workspace tests, release build, GUI smoke, and Ubuntu/macOS/Windows workspace tests. Directory-entry hardening commit `c0e0289` passed the local gate but its hosted run `32744806104` exposed a Windows-only parallel GUI-test path collision caused by timestamp-only names; commit `d904e76` replaced that naming with a process-local atomic counter and passed hosted run `32745159200` across all configured jobs. Commit `ea837b9` (`feat: bound materialized document count`) added write/import/replay count enforcement, CLI configuration, lower-budget replay rejection, quota reuse after deletion, and crypto design research; hosted VDB CI run `32751782508` passed documentation, format, Clippy, workspace tests, release build, GUI smoke, process-crash smoke, lockfile freshness, RustSec, and Ubuntu/macOS/Windows tests. Commit `dffd3da` (`feat: bound aggregate payload memory`) added aggregate payload accounting and quotas for writes, imports, deletes, replay, and health, with CLI controls and regression coverage; hosted VDB CI run `32753719103` passed all configured jobs.

These results demonstrate correctness for the tested MVP paths and add a best-effort Unix directory-entry synchronization step; they do not prove power-loss durability on every filesystem, production capacity, encryption, multi-process safety beyond the documented lock-file behavior, or model safety under a future LLM adapter.

## Current priorities

The WAL quota, document-count quota, aggregate payload-byte quota, baseline release packaging, durability matrix, nested equality queries, strict backup verification, atomic bounded import, fail-closed oversized-length handling, duplicate-snapshot rejection, all-boundary truncated-tail recovery fixture, sync_all file-synchronization attempt, Unix advisory-lock baseline, verified restore-to-new-path baseline, native Ubuntu/macOS/Windows workspace-test matrix, local read-only GUI baseline, and maintained-CBOR/cargo-deny remediation are implemented pending hosted revalidation. The next highest-value work is to expand the initial Unix process-crash smoke into the documented cross-platform interruption harness, then address the remaining P0 encryption/key-management evidence gate and byte-level working-set limits, followed by license/source policy and SBOM generation. In parallel, the project should execute the durability matrix with cross-platform crash/fuzz testing, design authenticated encryption and key management, replace the remaining in-memory materialized state with bounded segments/checkpoints, and close the platform-specific no-follow, non-Unix locking, network-filesystem, and directory-replacement gaps before adding an authenticated application API, remote serving, or model autonomy. The GUI does not change the production gate order.

## How to update this snapshot

When a feature changes state, update its row, add evidence, update the review date and commit, and revise the relevant roadmap or production-gap entry. When a test or CI result changes, update the validation paragraph with the exact command or run. When no implementation changes but a periodic review occurs, update the review date and record what was checked. The documentation and agent-tooling system is now implemented as a baseline: future work must follow [`AGENTS.md`](../AGENTS.md), [`CONTRIBUTING.md`](../CONTRIBUTING.md), [`docs/documentation-maintenance.md`](documentation-maintenance.md), [`docs/agent-tooling.md`](agent-tooling.md), and [`docs/loop-engineering.md`](loop-engineering.md).
